kind: Route
apiVersion: route.openshift.io/v1
metadata:
# set your user name
  name:  hindar-hello-world
spec:
  to:
    kind: Service
# set your user name
    name:  hindar-hello-world
    weight: 100
  port:
    targetPort: 8080
  wildcardPolicy: None